services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bibtex2ris
    ports:
      - "127.0.0.1:5862:5862"
    environment:
      - PORT=5862
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
